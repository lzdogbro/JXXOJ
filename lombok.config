# Lombok >= 1.16.20 stopped emitting @ConstructorProperties on generated
# constructors by default, which breaks Jackson deserialization of @Data/@Value
# classes with `final` fields (e.g. CommonResult) — Jackson needs the annotation
# to map JSON fields to constructor parameters. Restore the pre-1.16.20 behavior.
lombok.anyConstructor.addConstructorProperties = true
